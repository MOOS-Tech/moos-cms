'use strict';

/**
 * navbar-right-side service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::navbar-right-side.navbar-right-side');
