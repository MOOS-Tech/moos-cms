'use strict';

/**
 * services-title-point router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::services-title-point.services-title-point');
