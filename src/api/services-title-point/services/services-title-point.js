'use strict';

/**
 * services-title-point service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::services-title-point.services-title-point');
